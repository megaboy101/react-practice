Houses all React components.
Components are then divided by page.
All top level page components end in page.
All component files are capitalized by convention.
App.js is the overall head component, and is used in every other component (used for things like headers or footers)
index.js is the entry point
